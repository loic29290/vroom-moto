<main>
    <?php
    if (isset($data['locations'])) {
    ?>
        <section>
            <h1>Les locations de mes motos</h1>
            <div class="moto-list">
                <?php
                foreach ($data['locations'] as $location) {
                    $moto = $location->getMoto();
                ?>
                    <article class="moto-card">
                        <p><?= htmlspecialchars($moto->getMarque()); ?> <?= htmlspecialchars($moto->getModele()); ?> Début: <?= htmlspecialchars($location->getDebutFormate()); ?> Fin: <?= htmlspecialchars($location->getFinFormate()); ?></p>
                        <img src="<?= htmlspecialchars($moto->getImage()); ?>" alt="<?= htmlspecialchars($moto->getModele()); ?>">
                    </article>
                <?php
                }
                ?>
            </div>
        </section>
    <?php
    }
    ?>
</main>